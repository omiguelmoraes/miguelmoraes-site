---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Página não encontrada — Miguel Moraes">
  <div class="page">
    <span class="label">404</span>
    <h1 class="heading">Você clicou em algo que ainda não desenhei.</h1>
    <p class="body">Esta página não existe ou foi movida.</p>
    <a href="/" class="back">← Voltar para o início</a>
  </div>
</BaseLayout>

<style>
  .page {
    max-width: var(--layout-max-width);
    margin: 0 auto;
    padding: var(--layout-padding-y) var(--layout-padding-x);
    display: flex;
    flex-direction: column;
    gap: 12px;
    min-height: 100vh;
    justify-content: center;
  }

  .label {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-muted);
  }

  .heading {
    font-size: var(--font-size-title);
    font-weight: 400;
    color: var(--color-title);
    line-height: var(--line-height-title);
  }

  .body {
    font-size: var(--font-size-body);
    color: var(--color-body);
    line-height: var(--line-height-body);
  }

  .back {
    font-size: var(--font-size-body);
    color: var(--color-muted);
    text-decoration: none;
    margin-top: 8px;
  }

  @media (hover: hover) {
    .back:hover {
      color: var(--color-title);
    }
  }
</style>
